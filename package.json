{
  "name": "kai-bluebubbles-plugin",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && mkdir -p dist && mv src/backend.js dist/backend.js && cp src/frontend/index.js dist/frontend.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0"
  }
}
